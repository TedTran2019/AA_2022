<p style="color: green"><%= notice %></p>

<h1>Goals</h1>

<div id="goals">
  <% @goals.each do |goal| %>
    <p>
      <%= link_to goal.title, goal %>
    </p>
  <% end %>
</div>

<%= link_to "New goal", new_goal_path %>

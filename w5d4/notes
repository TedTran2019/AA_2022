Javascript
Confirm w/ MDN that compatible
Prototype vs Objects
arrow functions
Node
pry -> node
vscode js debugger -> byebug
zero, empty strings, undefined, null, and NaN are all false

Primitive types: nmbers, string, boolean, undefined, null. NOT AN OBJ
Behavior/State treated same vs diff (JS vs Ruby)
var/let/const/window/global
let -> block scope
const -> constants (also block scoped)
functions are just a special type of obj, can be invoked with ()
"use strict"
this -> reset on every function call
bind
that e.g just a normal variable

arrow functions don't create a new scope, anonymous
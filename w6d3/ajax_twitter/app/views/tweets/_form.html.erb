<h2>New Tweet!</h2>
<form class="tweet-compose" action="/tweets" method="POST">
  <input
      type="hidden"
      name="authenticity_token"
      value="<%= form_authenticity_token %>">

  <strong class="chars-left"></strong>
  <label>
    Content
    <textarea name="tweet[content]"></textarea>
  </label>
  <br>

  <ul class="tweet-mentions">
    <label class="tweet-mention">
      Mention
      <select name="tweet[mentioned_user_ids][]">
        <option></option>
        <% User.all.each do |user| %>
          <option value="<%= user.id %>"><%= user.username %></option>
        <% end %>
      </select>
      <a class="remove-mentioned-user" href="remove-mentioned-user">Remove</a>
    </label>
    <br>
  </ul>

  <input type="Submit" value="Post Tweet!">
</form>
